# Database Configuration (These are defaults, override in main .env or via Docker env vars)
# POSTGRES_SERVER=exercise-db
# POSTGRES_USER=exercise_user
# POSTGRES_PASSWORD=exercise_password
# POSTGRES_DB=exercise_db

# Admin Credentials (CHANGE THESE DEFAULTS in main .env or via Docker env vars!)
# EXERCISE_ADMIN_USERNAME=admin
# EXERCISE_ADMIN_PASSWORD=supersecretpassword

# Gemini Service URL (Internal Docker network URL)
# GEMINI_SERVICE_URL=http://gemini-service:8001/api/v1

# Service Configuration (Can be overridden by environment variables)
# PROJECT_NAME="Exercise Management Service"
# API_V1_STR="/api/v1"

# Uvicorn/Server settings (if running directly, often managed by Docker/Compose)
# HOST=0.0.0.0
# PORT=8002

# Secret key for JWT token generation (CHANGE THIS TO A STRONG RANDOM KEY in main .env or via Docker env vars!)
# Generate one using: openssl rand -hex 32
# EXERCISE_SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7
# EXERCISE_ALGORITHM=HS256
# EXERCISE_ACCESS_TOKEN_EXPIRE_MINUTES=30